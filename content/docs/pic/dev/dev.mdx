---
title: 本地开发
---

如果您需要 clone 到本地开发：

```shell
git clone https://github.com/besscroft/PicImpact.git

pnpm i
```

如果是第一次运行需要执行:

```shell
pnpm run dev:prisma
```

然后，您可以复制一份 `.env.example` 文件为 `.env`，并设置您的参数，然后执行：

```shell
pnpm run dev:turbopack
```

接着访问 http://localhost:3000 即可！
